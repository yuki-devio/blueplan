<!-- blueplan:auditing-permissions 산출물. 저장 위치: .claude/docs/permissions.md
     리포트와 달리 이 문서는 짧아야 한다 — 매 작업 전에 로드되는 문서다. 1페이지를 넘기지 말 것.
     상세 근거는 리포트에 두고 여기서는 링크만 건다. 시크릿 값은 여기에도 쓰지 않는다. -->

# 권한 경계 — <PROJECT_NAME>

마지막 감사: YYYY-MM-DD · 상세: `docs/security/permission-audit-YYYY-MM-DD.md`
갱신: blueplan:auditing-permissions 재실행

Claude: 이 프로젝트에서 자격증명·데이터베이스·배포·git 원격을 건드리는 작업을 하기 전에
이 문서를 읽는다. 아래는 권고가 아니라 이 프로젝트의 구속력 있는 경계다.

## 이 프로젝트의 현실

<!-- 한 문단. 왜 이 경계가 필요한지. 예: "이 저장소의 .env.local에는 프로덕션 Supabase의
     service_role 키가 있다. 이 키는 RLS를 우회하므로, 이 저장소에서 실행되는 모든 DB 접근은
     모든 사용자 데이터에 대한 무제한 권한을 갖는다." -->

<한 문단>

## 하지 말 것

<!-- 구체적인 명령/동작만. "조심할 것" 같은 문장은 넣지 않는다. 각 줄에 왜 그런지 한 조각. -->

| 하지 말 것 | 이유 |
|---|---|
| `<정확한 명령>` | <되돌릴 수 없는 결과> |
| `<정확한 명령>` | |

## 사용자에게 먼저 물어볼 것

<!-- 금지는 아니지만 승인 없이 진행하면 안 되는 것 -->

- <동작> — <무엇을 확인받아야 하는지>

## 안전한 대안

| 하려던 것 | 대신 이렇게 |
|---|---|
| `<위험한 명령>` | `<안전한 명령>` |

## 확인되지 않은 영역

<!-- 감사가 답하지 못한 것. 여기 있는 항목은 "안전하다고 가정하지 말 것"이라는 뜻이다. -->

- <항목> — 미확인. 건드리기 전에 사용자에게 확인할 것.

## 기록

이 문서가 실제 설정과 어긋난다고 판단되면(새 MCP 서버, 새 키, 권한 변경) 추측으로 고치지 말고
blueplan:auditing-permissions 재실행을 제안할 것.
